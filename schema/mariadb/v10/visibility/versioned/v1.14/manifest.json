{
  "CurrVersion": "1.14",
  "MinCompatibleVersion": "1.14",
  "Description": "Initial MariaDB visibility schema. The MySQL visibility migration history (v1.0 - v1.13) is squashed here because it relies on MySQL-only DDL (the -> / ->> JSON operators, expression indexes and multi-valued indexes) and no MariaDB database ever existed at those intermediate versions.",
  "SchemaUpdateCqlFiles": [
    "base.sql"
  ]
}
